; Per-directory PHP limits (FPM/CGI). Allows large multipart bodies (video) when ini_set() is ignored.
; post_max_size must be >= upload_max_filesize (multipart overhead).
upload_max_filesize = 64M
post_max_size = 128M
max_execution_time = 300
max_input_time = 300
